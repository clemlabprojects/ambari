{
  "capability": "hive",
  "label": "Hive Metastore / HiveServer2",
  "order": 40,
  "fields": [
    {
      "name": "metastoreUri",
      "label": "Hive Metastore URI",
      "type": "string",
      "placeholder": "thrift://hms.example.com:9083",
      "managedResolver": "hive.metastoreUri",
      "group": "Hive"
    },
    {
      "name": "hs2JdbcUrl",
      "label": "HiveServer2 JDBC URL",
      "type": "string",
      "placeholder": "jdbc:hive2://hs2.example.com:10000/",
      "managedResolver": "hive.hs2JdbcUrl",
      "group": "Hive"
    },
    {
      "name": "hs2HostPort",
      "label": "HiveServer2 host:port",
      "type": "string",
      "placeholder": "hs2.example.com:10001",
      "help": "HiveServer2 endpoint the OM connector dials. For a MANAGED context KDPS computes the HTTP-transport port from hive-site; for an EXTERNAL context, provide it here.",
      "managedResolver": "hive.hs2HostPort",
      "group": "Hive"
    },
    {
      "name": "scheme",
      "label": "Connection scheme",
      "type": "string",
      "placeholder": "hive+https",
      "help": "hive+https / hive+http / hive. For a MANAGED context KDPS derives it from hive.server2.transport.mode + use.SSL; for an EXTERNAL context, set it here.",
      "managedResolver": "hive.scheme",
      "group": "Hive"
    },
    {
      "name": "transportMode",
      "label": "HS2 transport mode",
      "type": "enum",
      "placeholder": "http",
      "help": "http / binary. For a MANAGED context KDPS reads hive.server2.transport.mode from hive-site (defaults to binary when unset). Superset picks the SQLAlchemy driver from this: binary \u2192 hive:// (PyHive), http \u2192 impala:// (impyla over HTTP). For an EXTERNAL context, set it here.",
      "managedResolver": "hive.transportMode",
      "group": "Hive",
      "options": [
        "binary",
        "http"
      ],
      "default": "binary"
    },
    {
      "name": "authMode",
      "label": "Authentication",
      "type": "enum",
      "placeholder": "kerberos",
      "help": "kerberos / ldap / none. For a MANAGED context KDPS derives it from hive.server2.authentication.",
      "managedResolver": "hive.authMode",
      "group": "Hive",
      "options": [
        "kerberos",
        "ldap",
        "none"
      ]
    },
    {
      "name": "rangerServiceName",
      "label": "Hive Ranger service name",
      "type": "string",
      "placeholder": "cl1_hive",
      "help": "Name of the Ranger service (repository) protecting this Hive. KDPS grants the deployed service's read access here (e.g. Superset column reflection, OpenMetadata ingestion). For a MANAGED context KDPS derives it as <cluster>_hive; for an EXTERNAL context set it to the backend cluster's Hive Ranger repo name.",
      "managedResolver": "hive.rangerServiceName",
      "group": "Hive"
    },
    {
      "name": "servicePrincipal",
      "label": "Hive service principal (SPN)",
      "type": "string",
      "appliesTo": "EXTERNAL",
      "placeholder": "hive/_HOST@CDP.EXAMPLE.COM",
      "help": "The Kerberos service principal of the backend Hive Metastore / HiveServer2 (e.g. CDP). Sets hive.metastore.service.principal in the Trino Hive catalog. Only needed for an EXTERNAL context whose Hive is in a different realm than the Ambari cluster.",
      "group": "Hive"
    },
    {
      "name": "clientPrincipal",
      "label": "Hive client principal",
      "type": "string",
      "appliesTo": "EXTERNAL",
      "placeholder": "trino/_HOST@CDP.EXAMPLE.COM",
      "help": "The Kerberos principal the deployed service authenticates AS to the backend Hive \u2014 this MUST match the principal inside the external keytab you provide at deploy time. Sets hive.metastore.client.principal / hive.hdfs.trino.principal in the Trino Hive catalog. EXTERNAL contexts only.",
      "group": "Hive"
    }
  ]
}
